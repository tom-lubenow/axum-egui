[package]
name = "todo-server"
version = "0.1.0"
edition = "2024"
publish = false

[[bin]]
name = "todo-server"
path = "src/main.rs"

[dependencies]
# Import frontend as library (for server function registration)
todo-frontend = { path = "../frontend", features = ["ssr"] }

# axum-egui framework
axum-egui = { path = "../../../axum-egui", features = ["server"] }

# Web framework
axum = { workspace = true }
tokio = { workspace = true }
tracing = { workspace = true }
tracing-subscriber = { workspace = true }

# Asset embedding (with path interpolation for build.rs env vars)
rust-embed = { workspace = true, features = ["interpolate-folder-path"] }

# Serialization
serde = { workspace = true }
serde_json = { workspace = true }

# Artifact dependency: the frontend WASM (RFC 3028)
# This automatically builds the frontend for wasm32 and provides the path
[build-dependencies]
axum-egui-build = { path = "../../../axum-egui-build" }
todo-frontend = { path = "../frontend", artifact = "cdylib", target = "wasm32-unknown-unknown", features = ["hydrate"] }
